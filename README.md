# no-trace-browser
Ghost Browser (Lab Edition) is a Python/PyQt WebEngine desktop browser for cybersecurity labs. It uses an ephemeral per-run profile, defaults to about:blank, blocks sensitive permissions, supports Tor/custom proxies with startup checks, and includes tabs, session reset, settings, and download modes.
